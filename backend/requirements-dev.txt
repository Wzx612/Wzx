# Test-only dependencies. Pulls in the runtime deps, then adds the test stack.
# CI installs this; the production Docker image installs requirements.txt only.
-r requirements.txt

pytest>=8.0.0
pytest-asyncio>=0.24.0
